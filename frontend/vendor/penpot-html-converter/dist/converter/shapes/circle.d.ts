import type { CircleShape, ShapeCommon } from '../../penpot.types';
import type { ConverterContext } from '../types';
export declare function renderCircle(shape: CircleShape, _parent: ShapeCommon | null, ctx: ConverterContext): string;
